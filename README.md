# 🧩 Saint Hub UI Library

**Saint Hub UI Library** — это настраиваемая библиотека для создания пользовательских интерфейсов (UI) в Roblox.  
Она позволяет быстро создавать окна, вкладки, секции, кнопки, переключатели, поля ввода, выпадающие списки и уведомления с анимациями и конфигурацией.

---

## 📘 Возможности

- Создание окон и вкладок  
- Кнопки, переключатели, инпуты и выпадающие списки  
- Контейнеры и секции  
- Анимации (`TweenService`)  
- Сохранение и загрузка конфигурации  
- Уведомления и цветовая тема  

---

## 📂 Структура библиотеки

Library
├── Window
│ └── Tabs
│ └── Sections
│ ├── Buttons
│ ├── Toggles
│ ├── Inputs
│ ├── Dropdowns
│ └── Containers

yaml
Копировать код

---

## ⚙️ Основные объекты и методы

### ### `Library`

Главный объект библиотеки.

#### Методы:
| Метод | Аргументы | Описание |
|-------|------------|----------|
| `Library:CreateWindow(name)` | `name: string` | Создаёт главное окно |
| `Library:CreateNotification(title, message, duration?)` | `title: string`, `message: string`, `duration: number?` | Создаёт уведомление |
| `Library:SaveConfig()` | – | Сохраняет состояние UI |
| `Library:LoadConfig()` | – | Загружает сохранённую конфигурацию |

---

### 🪟 `Window`

Окно, содержащее вкладки (Tabs).

#### Методы:
| Метод | Аргументы | Описание |
|-------|------------|----------|
| `Window:CreateTab(tabName)` | `tabName: string` | Создаёт вкладку |

**Возвращает:** объект [`Tab`](#-tab)

---

### 🗂️ `Tab`

Вкладка, содержащая секции.

#### Методы:
| Метод | Аргументы | Описание |
|-------|------------|----------|
| `Tab:CreateSection(sectionName)` | `sectionName: string` | Создаёт секцию |

**Возвращает:** объект [`Section`](#-section)

---

### 📦 `Section`

Секция — контейнер для UI-элементов.

#### Методы:
| Метод | Аргументы | Описание |
|-------|------------|----------|
| `Section:CreateButton(text, callback)` | `text: string`, `callback: function` | Создаёт кнопку |
| `Section:CreateToggle(text, defaultValue, callback)` | `text: string`, `defaultValue: boolean`, `callback: function` | Создаёт переключатель |
| `Section:CreateInput(placeholder, callback)` | `placeholder: string`, `callback: function` | Создаёт текстовое поле |
| `Section:CreateDropdown(text, options, defaultOption, callback)` | `text: string`, `options: {string}`, `defaultOption: string`, `callback: function` | Создаёт выпадающий список |
| `Section:CreateContainer(title)` | `title: string` | Создаёт вложенный контейнер |

---

### 🔘 `Button`

Кнопка с функцией обратного вызова.

#### Методы:
| Метод | Аргументы | Описание |
|-------|------------|----------|
| `Button:SetText(text)` | `text: string` | Изменяет текст кнопки |
| `Button:Click()` | – | Программно нажимает кнопку |

---

### 🔄 `Toggle`

Переключатель (вкл/выкл).

#### Свойства:
| Имя | Тип | Описание |
|-----|-----|-----------|
| `Toggle.Value` | `boolean` | Текущее состояние |

#### Методы:
| Метод | Аргументы | Описание |
|-------|------------|----------|
| `Toggle:Set(state)` | `state: boolean` | Устанавливает состояние |
| `Toggle:OnChanged(callback)` | `callback: function` | Добавляет обработчик изменения |

---

### ✏️ `Input`

Текстовое поле.

#### Свойства:
| Имя | Тип | Описание |
|-----|-----|-----------|
| `Input.Text` | `string` | Введённый текст |

#### Методы:
| Метод | Аргументы | Описание |
|-------|------------|----------|
| `Input:SetText(text)` | `text: string` | Изменяет текст |
| `Input:OnChanged(callback)` | `callback: function` | Добавляет обработчик изменения |

---

### 📋 `Dropdown`

Выпадающий список.

#### Свойства:
| Имя | Тип | Описание |
|-----|-----|-----------|
| `Dropdown.Selected` | `string` | Текущее выбранное значение |

#### Методы:
| Метод | Аргументы | Описание |
|-------|------------|----------|
| `Dropdown:AddOption(option)` | `option: string` | Добавляет вариант |
| `Dropdown:RemoveOption(option)` | `option: string` | Удаляет вариант |
| `Dropdown:Set(option)` | `option: string` | Устанавливает выбранное значение |

---

### 🧱 `Container`

Группирующий блок внутри секции.  
Поддерживает те же методы, что и `Section`.

---

## 🎨 Темы и стили

- Основной цвет хранится в `Library.ThemeColor`
- Анимации выполняются через `TweenService`
- Поддерживается адаптивная смена цвета при наведении и кликах

---

## 💾 Конфигурация

Библиотека сохраняет состояние всех элементов (Toggle, Dropdown, Input) в файл конфигурации.

| Метод | Описание |
|--------|-----------|
| `Library:SaveConfig()` | Сохраняет текущее состояние |
| `Library:LoadConfig()` | Загружает сохранённое состояние |

---

## 🔔 Уведомления

#### `Library:CreateNotification(title, message, duration?)`

Создаёт уведомление на экране.

| Аргумент | Тип | Описание |
|-----------|-----|----------|
| `title` | `string` | Заголовок уведомления |
| `message` | `string` | Текст уведомления |
| `duration` | `number` | Длительность отображения (по умолчанию 3 сек) |

---

## 💡 Пример использования

```lua
local UI = Library:CreateWindow("Saint Hub UI")

local MainTab = UI:CreateTab("Главная")
local Section = MainTab:CreateSection("Основные функции")

Section:CreateButton("Нажми", function()
    print("Кнопка нажата!")
end)

Section:CreateToggle("Режим", false, function(state)
    print("Режим:", state and "включён" or "выключен")
end)

Section:CreateInput("Введите имя", function(text)
    print("Введено:", text)
end)

Section:CreateDropdown("Выбор оружия", {"Меч", "Лук", "Магия"}, "Меч", function(option)
    print("Вы выбрали:", option)
end)
```
